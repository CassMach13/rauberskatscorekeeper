# Copie para .env e preencha no deploy (Vercel) ou exporte no shell.
# Nunca versionar o arquivo .env nem firebase-credentials.json.

# JSON completo da service account do Firebase Admin (uma linha ou multiline)
# Alternativa local: arquivo firebase-credentials.json na raiz (já no .gitignore)
FIREBASE_CREDENTIALS=
